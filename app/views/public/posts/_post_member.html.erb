 <div class="d-flex justify-content-between m-2">
  <div>
    <%= link_to image_tag(post.member.get_profile_image, size: '30x30', style:"border-radius: 15px;"), member_path(post.member.id) %>
    <%= link_to post.member.name, member_path(post.member.id) %>
  </div>
  <div>
    <!--投稿メンバーまたは管理者のみ表示-->
    <% if post.member == current_member || admin_signed_in? %>
      <%= link_to edit_post_path(post) do %>
        <i class="fas fa-cut"></i>
      <% end %>
      <%= link_to post, method: :delete, remote: true, data: {confirm: "この投稿を削除しますか？"} do %>
        <i class="far fa-trash-alt"></i>
      <% end %>
    <% end %>
  </div>
</div>