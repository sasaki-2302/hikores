<% if member_signed_in? %>
  <%= image_tag member.get_profile_image, size: '100x100' %>
  <%= link_to member.name, member_path(member.id) %><br>
  <%= link_to "フォロー: " + member.followings.count.to_s, member_followings_path(member.id) %><br>
  <%= link_to "フォロワー: " + member.followers.count.to_s, member_followers_path(member.id) %><br>
  <%= render "public/shared/follow_btn", member: member %>
  <% if current_member != member && current_member.following?(member) && member.following?(current_member) %>
    <%= link_to 'chatを始める', chat_path(member.id), class: "ml-3" %>
  <% end %>
  <% if current_member == member && member.name != "guestmember" %>
    <div>
      <%= link_to "プロフィール編集", edit_member_path(member.id) %>
    </div>
  <% end %>
  <%=link_to"お気に入り記事",favorites_member_path(member) %>
<% elsif admin_signed_in? %>
  <p>管理者にてログイン中</p>
<% else %>
  <p>
    当サイトはログインせずに<br>
    投稿の閲覧が可能です。<br>
    (一部、機能が制限されます)<br>
    <br>
    ご自身で投稿されたい方は<br>
    <%= link_to "こちら", new_member_session_path %>
    からログインして下さい。<br>
    <br>
    また
    <%= link_to "ゲストユーザー", new_member_session_path %>
    として<br>
    機能を体験することも<br>
    可能です。<br>
    (1時間後に自動的にログアウトされ、投稿内容も消去されます)
  </p>
<% end %>